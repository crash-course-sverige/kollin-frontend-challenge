export default function Title({ title }) {
  return (
    <span
      style={{
        fontSize: 24,
        color: "#FDFDFC",
        lineHeight: "34px",
      }}
    >
      {title}
    </span>
  );
}
