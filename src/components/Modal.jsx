const Modal = ({ text }) => {
    return (
        <div
            className="w-full h-dvh flex justify-center items-center"
        >
            {text}
        </div>
    );
}

export default Modal;
